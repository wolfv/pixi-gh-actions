# Generated by: python generate_recipe.py actions/checkout@v4.2.2
# Regenerate (to refresh sha256 or update entry points):
#   python generate_recipe.py actions/checkout@v4.2.2
#
# Files in this directory:
#   recipe.yaml   — rattler-build recipe
#   build.sh      — Unix build script
#   bld.bat       — Windows build script
#   checkout.sh   — Bash entry point (→ $PREFIX/bin/checkout)
#   checkout.ps1  — PowerShell entry point (→ $PREFIX/Scripts/checkout.ps1)
#   checkout.bat  — .bat shim that delegates to checkout.ps1 (Windows)
#
# Runtime deps: nodejs only — no Python, no PyYAML.

context:
  owner: actions
  repo: checkout
  version: "4.2.2"

package:
  name: gh-action-actions-checkout
  version: ${{ version }}

source:
  url: https://github.com/actions/checkout/archive/refs/tags/v4.2.2.tar.gz
  # Run `python generate_recipe.py actions/checkout@v4.2.2` to fill in the real sha256.
  sha256: 11bd71901bbe5b1630ceea73d27597364c9af683f2a14c45f8f5bd68bf5cde48

build:
  number: 0
  script: build.sh

requirements:
  run:
    - nodejs >=20

tests:
  - package_contents:
      files:
        - share/gh-actions/actions/checkout/action.yml
  - script:
      - checkout --help

about:
  homepage: https://github.com/actions/checkout
  license: MIT
  license_file: LICENSE
  summary: "GitHub Action: actions/checkout"
  description: |
    Action for checking out a repo so your workflow has access to it.
    Installs a `checkout` bash/PowerShell entry point that accepts all
    declared action inputs as flags, a JSON string, or a YAML/JSON file.
    The entry point invokes node directly — no Python dependency.
